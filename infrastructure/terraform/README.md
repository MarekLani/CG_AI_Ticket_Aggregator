# Terraform

Terraform configuration is intentionally not bootstrapped yet. It should be created as a dedicated high-risk/architectural issue after the first minimal application has been proven locally and an initial Azure runtime path has been validated.

Read `docs/standards/infrastructure/terraform.md` and `docs/architecture/deployment.md` first.
