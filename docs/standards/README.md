# Engineering standards

Standards contain durable, repeatable rules. A GitHub Issue defines desired behavior for one change; an approved implementation plan defines its implementation approach; an ADR records a concrete durable decision or approved exception.

If a task conflicts with a standard, the agent must not silently override the standard. Escalate for human decision and record an approved exception appropriately.
